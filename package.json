{
  "name": "json-ld.org",
  "private": true,
  "description": "json-ld.org homepage",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
